{
  "name": "tim-and-jen-recipe-book",
  "version": "1.0.0",
  "description": "A collection of vegetarian and gluten-free friendly recipes",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "eleventy",
    "deploy": "npm run build && npx wrangler pages deploy _site/recipes",
    "dev": "npm run build && serve _site"
  },
  "private": true,
  "license": "CC-BY-SA-3.0",
  "authors": [
    "Tim",
    "Jen"
  ],
  "keywords": [],
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "serve": "^14.2.0"
  }
}
